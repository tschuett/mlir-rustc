#include "Sema/Sema.h"

namespace rust_compiler::sema {

bool Sema::isReachable(std::shared_ptr<ast::VisItem>,
                       std::shared_ptr<ast::VisItem>) {
  assert(false);  
}

} // namespace rust_compiler::sema
